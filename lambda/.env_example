# Copy this file to .env and fill in your values
# Usage: source .env && ./deploy.sh

# Astro deployment API URL (without /api/v2)
# Get this from: astro deployment inspect -n 'Your Deployment' --key metadata.airflow_api_url
# Then remove the /api/v2 suffix and add https://
ASTRO_API_URL=https://your-org.astronomer.run/your-deployment-id

# Astro API Token
# Create one at: https://cloud.astronomer.io/ -> Organization Settings -> API Tokens
# Or use: astro token create --name "HITL Slack Lambda" --role ORGANIZATION_OWNER
ASTRO_API_TOKEN=your-astro-api-token

# Webhook Secret - shared secret between Airflow plugin and Lambda
# Generate with: openssl rand -hex 32
# This same value must be set as HITL_WEBHOOK_SECRET in your Astro deployment
WEBHOOK_SECRET=your-webhook-secret

# Slack Signing Secret - from your Slack app's "Basic Information" page
# Found at: https://api.slack.com/apps/<your-app-id>/general
# Under "App Credentials" -> "Signing Secret"
SLACK_SIGNING_SECRET=your-slack-signing-secret

# Slack Bot Token - from your Slack app's "OAuth & Permissions" page
# After installing the app, copy the "Bot User OAuth Token" (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack Channel ID - the channel where HITL messages will be sent
# Right-click on a channel in Slack -> "View channel details" -> scroll down to see Channel ID
SLACK_CHANNEL=C0123456789

# AWS Region for Lambda deployment
AWS_REGION=us-east-1

# SAM stack name
STACK_NAME=hitl-slack-webhook

# S3 bucket for Lambda deployment artifacts
# Leave empty to auto-create a managed bucket (recommended for first-time setup)
# Or specify your own bucket name
S3_BUCKET=

