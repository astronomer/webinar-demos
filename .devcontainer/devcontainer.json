{
	"name": "Astro Runtime Devcontainer",
	"build": {
		"context": "..",
		"dockerfile": "../Dockerfile"
	},
	"postCreateCommand": "ln -sfn ${containerWorkspaceFolder}/include /usr/local/airflow/include && airflow db migrate",
	"remoteEnv": {
		"AIRFLOW__CORE__DAGS_FOLDER": "${containerWorkspaceFolder}/dags"
	},
	"customizations": {
		"vscode": {
			"settings": {
				"python.defaultInterpreterPath": "/usr/local/bin/python"
			},
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"ms-vscode.live-server"
			]
		}
	}
}
